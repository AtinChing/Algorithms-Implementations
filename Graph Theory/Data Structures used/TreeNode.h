// Simple binary search tree node declaration that will be used a lot in the other graph theory algorithms so imported from here
typedef struct {
    int val;
    TreeNode* left;
    TreeNode* right;
} TreeNode;
